# Template file for 'brgvos-evo-dracut-conf'
# Content is packaged from the brgvos-includedir category archives in the brgvos-void repo.
pkgname=brgvos-evo-dracut-conf
version=1.0
revision=1
hostmakedepends="tar"
short_desc="BRGVOS xbps dracut configuration for Slimbook EVO laptop"
maintainer="Florin Tanasă <florin.tanasa@gmail.com>"
license="GPL-3.0-or-later"
homepage="https://github.com/florintanasa/brgvos-void"
#distfiles="https://github.com/florintanasa/brgvos-void/releases/download/v${version}/brgvos-evo-dracut-conf.tar.gz"
distfiles="https://github.com/florintanasa/brgvos-void/raw/refs/heads/work/files/includedir/brgvos-evo-dracut-conf.tar.gz"
checksum="c2de9c01e0681b1f10d9ca90b6b64690617a421d2235124ae7009ba15c193613"
# TODO: create GitHub release v${version} on florintanasa/brgvos-void and attach brgvos-evo-dracut-conf.tar.gz
#       (archive generated by the matching files/includedir/brgvos-*.sh script).
skip_extraction=yes
nostrip=yes
noverifyrdeps=yes

do_install() {
	tar -C "$DESTDIR" -xzf ${XBPS_SRCDISTDIR}/${pkgname}-${version}/brgvos-evo-dracut-conf.tar.gz
	# Fix the rights - eliminate the written right
	find "${DESTDIR}/" -type f -exec chmod 644 {} +
	find "${DESTDIR}/" -type d -exec chmod 755 {} +
}

