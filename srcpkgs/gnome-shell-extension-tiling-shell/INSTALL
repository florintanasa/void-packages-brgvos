#!/bin/sh
# This script runs after the package files are unpacked on the target system
case "$1" in
post)
    echo "Compiling GSettings schemas for Tiling Shell..."
    if [ -d /usr/share/gnome-shell/extensions/tilingshell@ferrarodomenico.com/schemas ]; then
        glib-compile-schemas /usr/share/gnome-shell/extensions/tilingshell@ferrarodomenico.com/schemas
    fi
    ;;
esac
