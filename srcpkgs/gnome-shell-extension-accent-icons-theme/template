# Template file for 'gnome-shell-extension-accent-icons-theme'
pkgname=gnome-shell-extension-accent-icons-theme
version=4
revision=2
hostmakedepends="unzip"
depends="gnome-shell glib"
short_desc="Makes icons follow system accent color and color scheme Light or Dark"
maintainer="Florin Tanasă <florin.tanasa@gmail.com>"
license="GPL-2.0"
homepage="https://extensions.gnome.org"
distfiles="${homepage}/extension-data/accent-icons-themebrgvos.v${version}.shell-extension.zip
 https://raw.githubusercontent.com/florintanasa/accent-icons-theme/refs/heads/main/LICENSE.md"

checksum="b5da87b05e7f6772ccddf6e42c657aa0617d7dc9da03c6215a1a52db29415387
 addb1c67a9dc2334f5d3f5ee416451266cf040d787070903cca6b9d99d6331a3"

do_extract() {
	# Unzip the official .zip archive into the temporary working directory of xbps-src
	unzip -d ${wrksrc} ${XBPS_SRCDISTDIR}/${pkgname}-${version}/accent-icons-themebrgvos.v${version}.shell-extension.zip
	# Copy LICENSE.md
	cp ${XBPS_SRCDISTDIR}/${pkgname}-${version}/LICENSE.md ${wrksrc}/
}

do_install() {
	# Local variables
	local uuid="accent-icons-theme@brgvos"
	local extdir="usr/share/gnome-shell/extensions/${uuid}"

	# 1. Install License
	vlicense LICENSE.md

	# 2. Create the global system extensions path inside the package
	vmkdir "${extdir}"
	
	# 3. Copy all files extracted from zip directly to system destination
	vcopy "*" "${extdir}"

	# 4. Make clean
	rm -f "${DESTDIR}/${extdir}/LICENSE.md"

	# 5. Ensure schemas directory exists (required for compilation)
	vmkdir "${extdir}/schemas"

	# 6. Fix the rights - eliminate the written right
	find "${DESTDIR}/${extdir}" -type f -exec chmod 644 {} +
	find "${DESTDIR}/${extdir}" -type d -exec chmod 755 {} +
}
