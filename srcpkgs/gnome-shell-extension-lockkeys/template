# Template file for 'gnome-shell-extension-lockkeys'
pkgname=gnome-shell-extension-lockkeys
version=69
revision=1
hostmakedepends="unzip"
depends="gnome-shell glib"
short_desc="Numlock & Capslock status on the panel"
maintainer="Florin Tanasă <florin.tanasa@gmail.com>"
license="GPL-2.0"
homepage="https://extensions.gnome.org"
distfiles="${homepage}/extension-data/lockkeysvaina.lt.v${version}.shell-extension.zip"

checksum="d50d793a739e8119d96cd3c862e3aebb7434cfb64ee95799ad0563547b066219"

do_extract() {
	# Unzip the official .zip archive into the temporary working directory of xbps-src
	unzip -d ${wrksrc} ${XBPS_SRCDISTDIR}/${pkgname}-${version}/lockkeysvaina.lt.v${version}.shell-extension.zip
}

do_install() {
	# The exact UUID used in the BRGV-OS distribution
	local uuid="lockkeys@vaina.lt"
	local extdir="usr/share/gnome-shell/extensions/${uuid}"

	# 1. Create the global system extensions path inside the package
	vmkdir "${extdir}"
	
	# 2. Copy all files extracted from zip directly to system destination
	vcopy "*" "${extdir}"

	# 3. Ensure schemas directory exists (required for compilation)
	vmkdir "${extdir}/schemas"
}
